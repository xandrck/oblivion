.col-md-4.col-md-offset-4
  .text-center
    %h3
      - if params[:action] == 'new' || params[:action] == 'create'
        New Storage
      - elsif params[:action] == 'edit' || params[:action] == 'update'
        = "Edit <b>#{@storage.title}</b>".html_safe

  = simple_form_for @storage, :url => @storage.id.present? ? storage_path : storages_path do |f|
    .form-group
      = f.text_field :title, placeholder: 'Name', class: 'form-control'
    .form-group
      = f.text_area :url, placeholder: 'URL', class: 'form-control'
    .form-group.col-md-12
      .controls.text-center
        - if params[:action] == 'edit' || params[:action] == 'update'
          = f.submit 'Refresh', class: 'btn btn-warning'
        - else
          = f.submit 'Create', class: 'btn btn-success'
